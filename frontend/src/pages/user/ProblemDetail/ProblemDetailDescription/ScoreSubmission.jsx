import { Box, TableCell, Typography } from "@mui/material";
import React from "react";

export default function ScoreSubmission() {
  const hihi = 0;
  return (
    <TableCell>
    <Box>
      <Typography fontSize={20}>Điểm của bạn: {hihi}</Typography>
    </Box>
    </TableCell>
  );
}
